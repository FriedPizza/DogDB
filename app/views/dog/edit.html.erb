<h1>Edit Dog</h1>
 
<%= form_with(model: @dog, local: true) do |form| %>
 
  <% if @dog.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@dog.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @dog.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :breed_name %><br>
    <%= form.text_field :breed_name %>
  </p>
  
  <p>
    <%= form.label :size %><br>
    <%= form.text_field :size %>
  </p>

  <p>
    <%= form.label :maintenance_cost %><br>
    <%= form.text_field :maint_cost %>
  </p>

  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', dog_index_path %>