<h1>Edit Fur</h1>
 
<%= form_with(model: @fur, local: true) do |form| %>
 
  <% if @fur.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@fur.errors.count, "error") %> prohibited
        this fur entry from being saved:
      </h2>
      <ul>
        <% @fur.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :type_name %><br>
    <%= form.text_field :type_name %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', fur_index_path %>