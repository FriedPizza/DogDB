<h1>Edit Character</h1>
 
<%= form_with(model: @character, local: true) do |form| %>
 
  <% if @character.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@character.errors.count, "error") %> prohibited
        this character entry from being saved:
      </h2>
      <ul>
        <% @character.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :char_name %><br>
    <%= form.text_field :char_name %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', character_index_path %>